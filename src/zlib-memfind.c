#include <zlib.h>

#include "zlib-memfind.h"

handle_t zlib_compress(handle_t p, handle_t q, const imode_t mode) {
  return NULL;
}

handle_t zlib_decompress(handle_t p, handle_t q) {
  return NULL;
}

