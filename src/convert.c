#include "convert.h"

int dump_shlbrute8(const handle_t o, const unknown_t p, const size_t size) {
  int n = 0;

  return n;
}

int dump_shlbrute16(const handle_t o, const unknown_t p, const size_t size) {
  int n = 0;

  return n;
}

int dump_shrbrute8(const handle_t o, const unknown_t p, const size_t size) {
  int n = 0;

  return n;
}

int dump_shrbrute16(const handle_t o, const unknown_t p, const size_t size) {
  int n = 0;

  return n;
}

int dump_xorbrute8(const handle_t o, const unknown_t p, const size_t size) {
  int n = 0;

  return n;
}

int dump_xorbrute16(const handle_t o, const unknown_t p, const size_t size) {
  int n = 0;

  return n;
}

